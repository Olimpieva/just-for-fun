export type FoxImagesApiResponse = {
  image: string;
  link: string;
};

export type DogsImagesApiResponse = {
  fileSizeBytes: number;
  url: string;
};

export type Fox = {
  id: string;
  image: string;
};
