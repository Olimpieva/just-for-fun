export type FoxesApiResponse = {
  image: string;
  link: string;
};

export type DogsApiResponse = {
  fileSizeBytes: number;
  url: string;
};

export type GalleryItem = {
  id: string;
  image: string;
};
